import React from "react";
import cl from './registration.module.css';

export const RecoveryPassword = ( ) => {
    return(
        <div className={cl.recoveryPassword}>
            TestPage
        </div>
    )
}