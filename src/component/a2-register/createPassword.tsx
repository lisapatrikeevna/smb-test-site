import React from "react";
import cl from './registration.module.css';

export const Createpassword = () => {
    return(
        <div className={cl.createPassword}>
            TestPage
        </div>
    )
}